<p id="notice"><%= notice %></p>

<h1>Listing Guests</h1>

<table>
  <thead>
    <tr>
      <th><%= @guests.model.human_attribute_name("name") %></th>
      <th><%= @guests.model.human_attribute_name("age") %></th>
      <th><%= @guests.model.human_attribute_name("gender") %></th>
      <th><%= @guests.model.human_attribute_name("category") %></th>
      <th><%= @guests.model.human_attribute_name("street") %></th>
      <th><%= @guests.model.human_attribute_name("neighborhood") %></th>
      <th><%= @guests.model.human_attribute_name("cep") %></th>
      <th><%= @guests.model.human_attribute_name("street_number") %></th>
      <th><%= @guests.model.human_attribute_name("state") %></th>
      <th><%= @guests.model.human_attribute_name("phone") %></th>
      <th colspan="3"></th>
    </tr>
  </thead>

  <tbody>
    <% @guests.each do |guest| %>
    <tr>
      <td><%= guest.name %></td>
      <td><%= guest.age %></td>
      <td><%= guest.gender.name %></td>
      <td><%= guest.category.name %></td>
      <td><%= guest.street %></td>
      <td><%= guest.neighborhood %></td>
      <td><%= guest.cep %></td>
      <td><%= guest.street_number %></td>
      <td><%= guest.state %></td>
      <td><%= guest.phone %></td>
      <td><%= link_to 'Show', guest %></td>
      <td><%= link_to 'Edit', edit_guest_path(guest) %></td>
      <td><%= link_to 'Destroy', guest, method: :delete, data: { confirm: 'Are you sure?' } %></td>
    </tr>
    <% end %>
  </tbody>
</table>

<br>

<%= link_to 'New Guest', new_guest_path %>